---
// src/components/layout/Footer.astro
import { 
  Github, Instagram, Linkedin, Mail, MessageCircle, Phone, MapPin, 
  Clock, Code, Rocket, Star, ArrowRight, Send, Heart, Zap
} from 'lucide-react';

const currentYear = new Date().getFullYear();

const quickLinks = [
  { name: "Inicio", href: "/" },
  { name: "Proyectos", href: "/proyectos" },
  { name: "Sobre Nosotros", href: "/sobre-mi" },
  { name: "Contacto", href: "/contacto" }
];

const services = [
  { name: "Desarrollo Web", href: "/proyectos#web" },
  { name: "Apps Mobile", href: "/proyectos#mobile" },
  { name: "Backend APIs", href: "/proyectos#backend" },
  { name: "UI/UX Design", href: "/proyectos#design" }
];

const technologies = [
  { name: "React", href: "#" },
  { name: "React Native", href: "#" },
  { name: "Node.js", href: "#" },
  { name: "Astro", href: "#" },
  { name: "MongoDB", href: "#" },
  { name: "TypeScript", href: "#" },
  { name: "Astro", href: "#"}
];

const socialLinks = [
  {
    name: "Instagram",
    href: "https://www.instagram.com/lambdacodestudio/",
    icon: Instagram,
    color: "hover:text-pink-400"
  },
  {
    name: "LinkedIn",
    href: "#",
    icon: Linkedin,
    color: "hover:text-blue-400"
  },
  {
    name: "GitHub",
    href: "https://github.com/LambdaCodeStudio",
    icon: Github,
    color: "hover:text-gray-300"
  },
  {
    name: "Email",
    href: "mailto:lambdacodestudio@gmail.com",
    icon: Mail,
    color: "hover:text-violet-400"
  }
];

const contactInfo = [
  {
    icon: Phone,
    text: "+54-2227-478196",
    href: "tel:+5492227478196"
  },
    {
    icon: Phone,
    text: "+54-011-50220157",
    href: "tel:+5401150220157"
  },
  {
    icon: Mail,
    text: "lambdacodestudio@gmail.com",
    href: "mailto:lambdacodestudio@gmail.com"
  },
  {
    icon: MapPin,
    text: "La Plata, Buenos Aires",
    href: "#"
  }
];

const stats = [
  { value: "7+", label: "Proyectos" },
  { value: "3+", label: "Años" },
  { value: "99%", label: "Satisfacción" },
  { value: "24h", label: "Respuesta" }
];
---

<footer class="relative bg-black border-t border-gray-800 overflow-hidden">
  <!-- Background Effects -->
  <div class="absolute inset-0 bg-gradient-to-t from-violet-900/10 via-transparent to-transparent"></div>
  <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_bottom,rgba(120,119,198,0.05),transparent)]"></div>
  
  <!-- Decorative Elements -->
  <div class="absolute top-0 left-1/4 w-32 h-32 bg-violet-500/5 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 right-1/4 w-48 h-48 bg-fuchsia-500/5 rounded-full blur-3xl"></div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Newsletter Section -->
    <div class="py-16 border-b border-gray-800">
      <div class="text-center max-w-4xl mx-auto">
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-violet-500/10 rounded-full border border-violet-500/20 mb-6">
          <Rocket className="w-4 h-4 text-violet-400" />
          <span class="text-violet-300 text-sm font-medium">Mantente Conectado</span>
        </div>
        
        <h2 class="text-3xl sm:text-4xl font-bold text-white mb-4">
          ¿Tienes una idea increíble?
        </h2>
        <p class="text-xl text-gray-400 mb-8">
          Conversemos sobre cómo podemos transformarla en una 
          <span class="text-violet-400 font-semibold">solución digital exitosa</span>
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center max-w-2xl mx-auto">
          <a 
            href="https://wa.me/5492227478196"
            class="group relative px-8 py-4 bg-gradient-to-r from-green-500 to-emerald-500 text-white rounded-xl font-medium hover:from-green-600 hover:to-emerald-600 transition-all duration-300 hover:scale-105 inline-flex items-center justify-center gap-2"
          >
            <MessageCircle className="w-5 h-5" />
            <span>Contactar por WhatsApp</span>
          </a>
          
          <a 
            href="/contacto"
            class="px-8 py-4 bg-gray-900/50 text-white rounded-xl font-medium border border-violet-500/20 hover:bg-violet-500/10 hover:border-violet-500/50 transition-all duration-300 inline-flex items-center justify-center gap-2"
          >
            <Send className="w-5 h-5" />
            <span>Ver Más Opciones</span>
          </a>
        </div>
      </div>
    </div>

    <!-- Main Footer Content -->
    <div class="py-16">
      <div class="grid grid-cols-1 lg:grid-cols-4 gap-8 lg:gap-12">
        
        <!-- Company Info -->
        <div class="lg:col-span-1">
          <div class="flex items-center gap-3 mb-6">
            <div class="w-10 h-10 bg-gradient-to-r from-violet-500 to-fuchsia-500 rounded-xl flex items-center justify-center">
              <Code className="w-6 h-6 text-white" />
            </div>
            <h3 class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-violet-500 to-fuchsia-500">
              Lambda Code Studio
            </h3>
          </div>
          
          <p class="text-gray-400 mb-6 leading-relaxed">
            Transformamos ideas en soluciones digitales innovadoras. 
            Desarrollamos aplicaciones web, móviles y sistemas que impulsan el crecimiento de tu negocio.
          </p>
          
          <!-- Stats -->
          <div class="grid grid-cols-2 gap-4 mb-6">
            {stats.map((stat) => (
              <div class="text-center p-3 bg-gray-900/30 rounded-lg border border-violet-500/10">
                <div class="text-lg font-bold text-white">{stat.value}</div>
                <div class="text-xs text-gray-400">{stat.label}</div>
              </div>
            ))}
          </div>
          
          <!-- Social Links -->
          <div class="flex gap-4">
            {socialLinks.map((social) => (
              <a 
                href={social.href}
                class={`p-3 bg-gray-900/30 rounded-lg text-gray-400 ${social.color} transition-all duration-300 hover:scale-110 hover:bg-gray-800/50 border border-gray-700/50 hover:border-violet-500/30`}
                aria-label={social.name}
                target={social.href.startsWith('http') ? '_blank' : '_self'}
                rel={social.href.startsWith('http') ? 'noopener noreferrer' : ''}
              >
                <social.icon className="w-5 h-5" />
              </a>
            ))}
          </div>
        </div>
        
        <!-- Quick Links -->
        <div>
          <h4 class="text-lg font-semibold text-white mb-6 flex items-center gap-2">
            <ArrowRight className="w-5 h-5 text-violet-400" />
            Navegación
          </h4>
          <ul class="space-y-3">
            {quickLinks.map((link) => (
              <li>
                <a 
                  href={link.href} 
                  class="text-gray-400 hover:text-violet-400 transition-colors duration-300 hover:translate-x-1 inline-block"
                >
                  {link.name}
                </a>
              </li>
            ))}
          </ul>
        </div>
        
        <!-- Services -->
        <div>
          <h4 class="text-lg font-semibold text-white mb-6 flex items-center gap-2">
            <Zap className="w-5 h-5 text-fuchsia-400" />
            Servicios
          </h4>
          <ul class="space-y-3">
            {services.map((service) => (
              <li>
                <a 
                  href={service.href} 
                  class="text-gray-400 hover:text-fuchsia-400 transition-colors duration-300 hover:translate-x-1 inline-block"
                >
                  {service.name}
                </a>
              </li>
            ))}
          </ul>
          
          <h5 class="text-md font-semibold text-white mt-8 mb-4">Tecnologías</h5>
          <div class="flex flex-wrap gap-2">
            {technologies.map((tech) => (
              <span class="px-3 py-1 bg-violet-500/10 text-violet-300 rounded-lg text-sm border border-violet-500/20 hover:bg-violet-500/20 transition-colors duration-300">
                {tech.name}
              </span>
            ))}
          </div>
        </div>
        
        <!-- Contact Info -->
        <div>
          <h4 class="text-lg font-semibold text-white mb-6 flex items-center gap-2">
            <MessageCircle className="w-5 h-5 text-pink-400" />
            Contacto
          </h4>
          <ul class="space-y-4">
            {contactInfo.map((contact) => (
              <li>
                <a 
                  href={contact.href}
                  class="flex items-center gap-3 text-gray-400 hover:text-pink-400 transition-colors duration-300 group"
                  target={contact.href.startsWith('http') ? '_blank' : '_self'}
                  rel={contact.href.startsWith('http') ? 'noopener noreferrer' : ''}
                >
                  <div class="w-8 h-8 bg-gray-900/30 rounded-lg flex items-center justify-center group-hover:bg-pink-500/20 transition-colors duration-300">
                    <contact.icon className="w-4 h-4" />
                  </div>
                  <span class="group-hover:translate-x-1 transition-transform duration-300">
                    {contact.text}
                  </span>
                </a>
              </li>
            ))}
          </ul>
          
          <!-- Business Hours -->
          <div class="mt-8 p-4 bg-gray-900/30 rounded-xl border border-gray-700/50">
            <div class="flex items-center gap-2 mb-3">
              <Clock className="w-4 h-4 text-violet-400" />
              <span class="text-white font-medium">Horarios de Atención</span>
            </div>
            <div class="space-y-1 text-sm text-gray-400">
              <div>Lun - Vie: 8:00 - 20:00</div>
              <div>Sábados: 8:00 - 14:00</div>
              <div class="text-violet-400">Respuesta 24/7 por email</div>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Bottom Bar -->
    <div class="py-8 border-t border-gray-800">
      <div class="flex flex-col md:flex-row items-center justify-between gap-4">
        <div class="flex items-center gap-2 text-gray-400">
          <span>© {currentYear} Lambda Code Studio.</span>
          <span>Hecho con</span>
          <Heart className="w-4 h-4 text-red-400 fill-current animate-pulse" />
          <span>en Argentina</span>
        </div>
        
        <div class="flex items-center gap-6 text-sm text-gray-400">       
          <div class="flex items-center gap-2">
            <Star className="w-4 h-4 text-yellow-400 fill-current" />
            <span>Calidad Garantizada :)</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</footer>

<script>
  // Smooth scroll for internal links
  document.addEventListener('DOMContentLoaded', () => {
    const footerLinks = document.querySelectorAll('footer a[href^="#"], footer a[href^="/"]');
    
    footerLinks.forEach(link => {
      if (link.href.includes('#') && !link.href.startsWith('http')) {
        link.addEventListener('click', function(e) {
          const target = document.querySelector(this.getAttribute('href'));
          if (target) {
            e.preventDefault();
            target.scrollIntoView({
              behavior: 'smooth',
              block: 'start'
            });
          }
        });
      }
    });

    // Add fade-in animation for footer sections
    const footerSections = document.querySelectorAll('footer > div > div');
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }
      });
    }, observerOptions);

    footerSections.forEach((section, index) => {
      section.style.opacity = '0';
      section.style.transform = 'translateY(20px)';
      section.style.transition = `opacity 0.6s ease ${index * 0.1}s, transform 0.6s ease ${index * 0.1}s`;
      observer.observe(section);
    });
  });
</script>

<style>
  /* Custom animations */
  @keyframes pulse-heart {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }

  .animate-pulse-heart {
    animation: pulse-heart 2s ease-in-out infinite;
  }

  /* Hover effects for social links */
  footer a:hover {
    transform: translateY(-2px);
  }

  /* Gradient text animation */
  .gradient-text-animated {
    background: linear-gradient(-45deg, #8b5cf6, #ec4899, #06b6d4, #10b981);
    background-size: 400% 400%;
    animation: gradient-shift 6s ease infinite;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @keyframes gradient-shift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  /* Smooth transitions */
  footer * {
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }
</style>