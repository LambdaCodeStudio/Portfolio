---
// src/pages/proyectos.astro
import Layout from '../components/layout/Layout.astro';
import { ArrowDown, Github, ExternalLink, Calendar, Users, Code, Rocket, Star } from 'lucide-react';

const categories = [
  "Todos", "Web Apps", "Mobile", "Backend", "Frontend", "PWA"
];

const allProjects = [
  {
    id: 1,
    title: "🦷 Odonto Care",
    shortDescription: "Aplicación completa para gestión de consultorios odontológicos",
    fullDescription: `Odonto Care es una solución integral diseñada específicamente para modernizar y optimizar la gestión de consultorios odontológicos. La aplicación combina tecnología de vanguardia con una interfaz intuitiva para proporcionar todas las herramientas necesarias para el manejo eficiente de pacientes y procedimientos dentales.

    La plataforma permite llevar historias clínicas digitales completas, con odontogramas interactivos, sistema de recordatorios automatizados, gestión de citas, control de pagos y prestaciones, y generación de reportes detallados. Todo esto respaldado por un sistema de seguridad robusto que cumple con estándares médicos de privacidad.`,
    tags: ["React", "Node.js", "Express.js", "MongoDB", "JWT", "Socket.io", "Material-UI"],
    technologies: {
      frontend: ["React 18", "Material-UI", "Chart.js", "Socket.io Client"],
      backend: ["Node.js", "Express.js", "MongoDB", "JWT", "Bcrypt"],
      tools: ["VS Code", "Postman", "Git", "Figma"]
    },
    features: [
      "Historias clínicas digitales completas",
      "Odontograma interactivo y visual",
      "Sistema de recordatorios automáticos",
      "Gestión integral de citas y turnos",
      "Control de pagos y prestaciones",
      "Reportes y estadísticas avanzadas",
      "Seguridad médica certificada"
    ],
    image: "odontoCare.png",
    gallery: ["odonto1.jpg", "odonto2.jpg", "odonto3.jpg"],
    gradient: "from-blue-500 to-cyan-500",
    category: "Web Apps",
    status: "Completado",
    duration: "4 meses",
    client: "Consultorios Odontológicos",
    year: "2024",
    github: "#",
    demo: "#",
    testimonial: {
      text: "Odonto Care transformó completamente la gestión de nuestro consultorio. La digitalización de las historias clínicas nos ahorró horas de trabajo.",
      author: "Dr. María González",
      position: "Odontóloga"
    }
  },
  {
    id: 2,
    title: "🛎️ CleanOrg",
    shortDescription: "Sistema inteligente de gestión de tareas para hoteles y empresas",
    fullDescription: `CleanOrg es una plataforma revolucionaria que automatiza la gestión de tareas en hoteles, pensiones y empresas de servicios. Utiliza algoritmos inteligentes para distribuir responsabilidades de manera equitativa, monitoreando el cumplimiento y optimizando la productividad del equipo.

    El sistema incorpora gamificación mediante un sistema de recompensas que motiva a los empleados, redistribuye automáticamente las tareas no completadas, y proporciona análisis detallados del rendimiento. Es la solución perfecta para establecimientos que buscan mejorar su eficiencia operativa.`,
    tags: ["React", "Node.js", "Express.js", "MongoDB", "PWA", "Material-UI", "Chart.js"],
    technologies: {
      frontend: ["React 18", "Material-UI", "PWA", "Chart.js", "Framer Motion"],
      backend: ["Node.js", "Express.js", "MongoDB", "JWT", "WebSockets"],
      tools: ["Docker", "Git", "Jest", "Lighthouse"]
    },
    features: [
      "Asignación automática inteligente de tareas",
      "Sistema de recompensas y gamificación",
      "Redistribución automática de responsabilidades",
      "Dashboard de métricas en tiempo real",
      "Notificaciones push personalizadas",
      "Modo offline para trabajo sin conexión",
      "Reportes de productividad detallados"
    ],
    image: "cleanOrg.png",
    gallery: ["clean1.jpg", "clean2.jpg", "clean3.jpg"],
    gradient: "from-green-500 to-emerald-500",
    category: "Web Apps",
    status: "En Producción",
    duration: "3 meses",
    client: "Hoteles y Empresas",
    year: "2024",
    github: "#",
    demo: "#",
    testimonial: {
      text: "CleanOrg aumentó nuestra eficiencia operativa en un 40%. El sistema de recompensas motivó increíblemente a nuestro equipo.",
      author: "Carlos Mendoza",
      position: "Gerente de Hotel"
    }
  },
  {
    id: 3,
    title: "📄 Descargo Mi Factura",
    shortDescription: "Plataforma de gestión simplificada de facturación empresarial",
    fullDescription: `Descargo Mi Factura es una solución completa que permite a las empresas ofrecer a sus clientes un acceso centralizado y seguro a toda su información de facturación. La plataforma elimina el papeleo tradicional y automatiza la gestión documental.

    Los clientes pueden visualizar, descargar y organizar todas sus facturas desde una interfaz moderna y responsiva. Incluye funciones de búsqueda avanzada, filtros personalizados, notificaciones automáticas, y un sistema de respaldo en la nube que garantiza la seguridad y disponibilidad de la información.`,
    tags: ["React Native", "Node.js", "Express.js", "MongoDB", "Firebase", "Expo"],
    technologies: {
      frontend: ["React Native", "Expo", "React Navigation", "AsyncStorage"],
      backend: ["Node.js", "Express.js", "MongoDB", "Firebase", "JWT"],
      tools: ["Expo CLI", "Android Studio", "XCode", "Firebase Console"]
    },
    features: [
      "Acceso móvil multiplataforma",
      "Descarga y visualización de facturas",
      "Búsqueda y filtros avanzados",
      "Notificaciones push automáticas",
      "Sincronización en la nube",
      "Modo offline disponible",
      "Seguridad bancaria certificada"
    ],
    image: "descargoMiFactura.png",
    gallery: ["factura1.jpg", "factura2.jpg", "factura3.jpg"],
    gradient: "from-purple-500 to-pink-500",
    category: "Mobile",
    status: "Completado",
    duration: "2.5 meses",
    client: "Empresas de Servicios",
    year: "2024",
    github: "#",
    demo: "#",
    playStore: "#",
    testimonial: {
      text: "Nuestros clientes ahora pueden acceder a sus facturas 24/7. El feedback ha sido extraordinario.",
      author: "Ana Rodríguez",
      position: "Directora Comercial"
    }
  },
  {
    id: 4,
    title: "🔒 Backend Seguro Enterprise",
    shortDescription: "Framework backend robusto con seguridad empresarial",
    fullDescription: `Nuestro framework backend personalizado representa años de experiencia condensados en una solución robusta y escalable. Diseñado siguiendo las mejores prácticas de la industria, proporciona una base sólida para cualquier aplicación empresarial.

    Incluye autenticación multi-factor, encriptación de datos, protección contra los ataques más comunes, sistema de logging avanzado, y arquitectura modular que permite escalabilidad horizontal. Es la base que utilizamos para todos nuestros proyectos empresariales.`,
    tags: ["Node.js", "Express.js", "MongoDB", "JWT", "Security", "Scalable"],
    technologies: {
      backend: ["Node.js 18+", "Express.js", "MongoDB", "Mongoose", "JWT"],
      security: ["Bcrypt", "Helmet", "Rate Limiting", "CORS", "Sanitization"],
      tools: ["Docker", "PM2", "Winston", "Jest", "Supertest"]
    },
    features: [
      "Autenticación JWT robusta",
      "Encriptación bcrypt de contraseñas",
      "Protección contra ataques OWASP Top 10",
      "Rate limiting y protección DoS",
      "Headers de seguridad automáticos",
      "Logging y monitoreo avanzado",
      "Arquitectura modular escalable"
    ],
    image: "logoLambda.jpeg",
    gallery: ["backend1.jpg", "backend2.jpg", "backend3.jpg"],
    gradient: "from-red-500 to-orange-500",
    category: "Backend",
    status: "Open Source",
    duration: "Desarrollo continuo",
    client: "Comunidad de Desarrolladores",
    year: "2023-2024",
    github: "https://github.com/LambdaCodeStudio/Plantilla-Backend",
    docs: "#"
  },
  {
    id: 5,
    title: "🔮 Frontend Astro + React",
    shortDescription: "Plantilla frontend moderna con Astro, React y Tailwind",
    fullDescription: `Nuestra plantilla frontend combina lo mejor de las tecnologías modernas: la velocidad de Astro, la flexibilidad de React y la elegancia de Tailwind CSS. Optimizada para rendimiento y experiencia de usuario, incluye TypeScript para mayor seguridad en desarrollo.

    La plantilla viene con componentes reutilizables, sistema de enrutado protegido, notificaciones integradas, y configuración lista para producción. Es la base perfecta para aplicaciones web modernas que requieren tanto velocidad como funcionalidad avanzada.`,
    tags: ["Astro", "React", "Tailwind CSS", "TypeScript", "Vite", "Modern"],
    technologies: {
      frontend: ["Astro 4.0", "React 18", "Tailwind CSS", "TypeScript", "Vite"],
      tools: ["ESLint", "Prettier", "Husky", "Lighthouse", "Vitest"],
      deployment: ["Vercel", "Netlify", "Docker", "CI/CD"]
    },
    features: [
      "Rendimiento ultra-optimizado",
      "Componentes React reutilizables",
      "Diseño responsive con Tailwind",
      "TypeScript para mayor seguridad",
      "Rutas protegidas integradas",
      "Sistema de notificaciones",
      "SEO optimizado automáticamente"
    ],
    image: "logoLambda.jpeg",
    gallery: ["frontend1.jpg", "frontend2.jpg", "frontend3.jpg"],
    gradient: "from-cyan-500 to-blue-500",
    category: "Frontend",
    status: "Open Source",
    duration: "Desarrollo continuo",
    client: "Comunidad de Desarrolladores",
    year: "2023-2024",
    github: "https://github.com/LambdaCodeStudio/Plantilla-Frontend",
    demo: "#"
  }
];
---

<Layout title="Proyectos | LambdaCodeStudio">
  <!-- Hero Section -->
  <div class="relative min-h-[60vh] flex items-center justify-center bg-black overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-br from-violet-900/30 via-fuchsia-900/20 to-pink-900/30"></div>
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(120,119,198,0.1),transparent)]"></div>
    
    <div class="relative text-center px-4 sm:px-6 lg:px-8 max-w-5xl mx-auto">
      <div class="inline-flex items-center gap-2 px-4 py-2 bg-violet-500/10 rounded-full border border-violet-500/20 mb-8">
        <Rocket className="w-4 h-4 text-violet-400" />
        <span class="text-violet-300 text-sm font-medium">Portfolio de Proyectos</span>
      </div>
      
      <h1 class="text-4xl sm:text-6xl lg:text-7xl font-bold mb-6">
        <span class="bg-clip-text text-transparent bg-gradient-to-r from-violet-500 via-fuchsia-500 to-pink-500">
          Nuestras
        </span>
        <br/>
        <span class="text-white">Creaciones Digitales</span>
      </h1>
      
      <p class="text-xl text-gray-400 mb-12 max-w-3xl mx-auto leading-relaxed">
        Explora nuestro portafolio de soluciones innovadoras. Cada proyecto representa nuestro compromiso con la excelencia técnica y el diseño centrado en el usuario.
      </p>
      
      <div class="flex items-center justify-center gap-8 text-gray-400">
        <div class="flex items-center gap-2">
          <Code className="w-5 h-5 text-violet-400" />
          <span>5+ Proyectos</span>
        </div>
        <div class="flex items-center gap-2">
          <Users className="w-5 h-5 text-fuchsia-400" />
          <span>20+ Clientes</span>
        </div>
        <div class="flex items-center gap-2">
          <Star className="w-5 h-5 text-pink-400" />
          <span>99% Satisfacción</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Filtros Mejorados -->
  <div class="bg-black py-12 border-b border-gray-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex flex-wrap justify-center gap-4">
        {categories.map((category) => (
          <button
            class="group px-6 py-3 rounded-full bg-gray-900/50 text-gray-300 hover:text-white border border-violet-500/20 hover:border-violet-500/50 transition-all duration-300 hover:bg-violet-500/10 hover:scale-105"
            data-category={category}
          >
            <span class="relative">
              {category}
              <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-gradient-to-r from-violet-500 to-fuchsia-500 group-hover:w-full transition-all duration-300"></span>
            </span>
          </button>
        ))}
      </div>
    </div>
  </div>

  <!-- Grid de Proyectos Mejorado -->
  <div class="bg-black py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12" id="projects-grid">
        {allProjects.map((project) => (
          <article 
            class="project-card group relative bg-gradient-to-br from-gray-900/50 to-gray-800/30 backdrop-blur-xl rounded-2xl overflow-hidden border border-gray-700/50 hover:border-violet-500/50 transition-all duration-500 hover:scale-[1.02] hover:shadow-2xl hover:shadow-violet-500/10"
            data-category={project.category}
          >
            {/* Status Badge */}
            <div class="absolute top-4 right-4 z-20">
              <span class={`px-3 py-1 rounded-full text-xs font-medium ${
                project.status === 'Completado' ? 'bg-green-500/20 text-green-400 border border-green-500/30' :
                project.status === 'En Producción' ? 'bg-blue-500/20 text-blue-400 border border-blue-500/30' :
                'bg-orange-500/20 text-orange-400 border border-orange-500/30'
              }`}>
                {project.status}
              </span>
            </div>

            {/* Imagen del Proyecto */}
            <div class="relative h-64 overflow-hidden">
              <img 
                src={project.image}
                alt={project.title}
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-60"></div>
              <div class={`absolute inset-0 bg-gradient-to-br ${project.gradient} opacity-0 group-hover:opacity-20 transition-opacity duration-500`}></div>
            </div>
            
            {/* Contenido */}
            <div class="p-8">
              <div class="flex items-start justify-between mb-4">
                <h3 class="text-2xl font-bold text-white group-hover:text-violet-400 transition-colors duration-300 line-clamp-2">
                  {project.title}
                </h3>
              </div>
              
              <p class="text-gray-400 mb-6 line-clamp-3 leading-relaxed">
                {project.shortDescription}
              </p>
              
              {/* Tecnologías */}
              <div class="flex flex-wrap gap-2 mb-6">
                {project.tags.slice(0, 4).map((tag) => (
                  <span class={`px-3 py-1 rounded-lg text-sm bg-gradient-to-r ${project.gradient} text-white font-medium shadow-lg opacity-90`}>
                    {tag}
                  </span>
                ))}
                {project.tags.length > 4 && (
                  <span class="px-3 py-1 rounded-lg text-sm bg-gray-700/50 text-gray-300">
                    +{project.tags.length - 4} más
                  </span>
                )}
              </div>

              {/* Meta información */}
              <div class="flex items-center justify-between mb-6 text-sm text-gray-400">
                <div class="flex items-center gap-4">
                  <div class="flex items-center gap-1">
                    <Calendar className="w-4 h-4" />
                    <span>{project.year}</span>
                  </div>
                  <div class="flex items-center gap-1">
                    <Users className="w-4 h-4" />
                    <span>{project.client}</span>
                  </div>
                </div>
                <span class="text-violet-400 font-medium">{project.category}</span>
              </div>

              {/* Acciones */}
              <div class="flex items-center justify-between">
                <div class="flex gap-3">
                  {project.github && (
                    <a 
                      href={project.github} 
                      class="p-3 rounded-xl bg-gray-800/50 text-gray-400 hover:text-white hover:bg-gray-700/50 transition-all duration-300 hover:scale-110"
                      title="Ver código"
                    >
                      <Github className="w-5 h-5" />
                    </a>
                  )}
                  {project.demo && (
                    <a 
                      href={project.demo} 
                      class="p-3 rounded-xl bg-gray-800/50 text-gray-400 hover:text-white hover:bg-gray-700/50 transition-all duration-300 hover:scale-110"
                      title="Ver demo"
                    >
                      <ExternalLink className="w-5 h-5" />
                    </a>
                  )}
                </div>
                
                <button 
                  class="group/btn relative px-6 py-3 bg-gradient-to-r from-violet-500 to-fuchsia-500 text-white rounded-xl font-medium hover:from-violet-600 hover:to-fuchsia-600 transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-violet-500/25"
                  onclick={`openProjectModal(${project.id})`}
                >
                  <span class="relative z-10">Ver Detalles</span>
                  <div class="absolute inset-0 bg-gradient-to-r from-violet-600 to-fuchsia-600 rounded-xl opacity-0 group-hover/btn:opacity-100 transition-opacity duration-300"></div>
                </button>
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </div>

  <!-- Modal de Proyecto -->
  <div id="project-modal" class="fixed inset-0 z-50 hidden">
    <div class="absolute inset-0 bg-black/80 backdrop-blur-md" onclick="closeProjectModal()"></div>
    <div class="relative h-full flex items-center justify-center p-4 overflow-y-auto">
      <div class="bg-gray-900/95 backdrop-blur-xl rounded-2xl max-w-6xl w-full my-8 border border-gray-700/50">
        <div class="flex flex-col lg:flex-row max-h-[85vh]">
          <!-- Contenido Principal -->
          <div class="flex-1 p-8 overflow-y-auto max-h-[85vh]">
            <div class="flex items-start justify-between mb-6 sticky top-0 bg-gray-900/95 backdrop-blur-xl py-4 -mx-8 px-8 z-10">
              <div>
                <h2 id="modal-title" class="text-3xl font-bold text-white mb-2"></h2>
                <p id="modal-client" class="text-violet-400 font-medium"></p>
              </div>
              <button 
                onclick="closeProjectModal()"
                class="p-2 rounded-xl bg-gray-800/50 text-gray-400 hover:text-white hover:bg-gray-700/50 transition-colors"
              >
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
              </button>
            </div>

            <!-- Imagen Principal -->
            <div class="relative mb-8 rounded-xl overflow-hidden">
              <img id="modal-image" src="" alt="" class="w-full h-64 object-cover">
              <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent"></div>
            </div>

            <!-- Descripción -->
            <div class="mb-8">
              <h3 class="text-xl font-semibold text-white mb-4">Descripción del Proyecto</h3>
              <div id="modal-description" class="text-gray-300 leading-relaxed"></div>
            </div>

            <!-- Características -->
            <div class="mb-8">
              <h3 class="text-xl font-semibold text-white mb-4">Características Principales</h3>
              <ul id="modal-features" class="grid grid-cols-1 md:grid-cols-2 gap-3"></ul>
            </div>

            <!-- Testimonial -->
            <div id="modal-testimonial" class="mb-8 p-6 bg-gradient-to-r from-violet-500/10 to-fuchsia-500/10 rounded-xl border border-violet-500/20">
            </div>
          </div>

          <!-- Sidebar -->
          <div class="lg:w-80 bg-gray-800/50 p-8 border-l border-gray-700/50 overflow-y-auto max-h-[85vh]">
            <!-- Estado y Meta -->
            <div class="mb-8">
              <div class="flex items-center justify-between mb-4">
                <span class="text-gray-400">Estado</span>
                <span id="modal-status" class="px-3 py-1 rounded-full text-xs font-medium"></span>
              </div>
              <div class="flex items-center justify-between mb-4">
                <span class="text-gray-400">Duración</span>
                <span id="modal-duration" class="text-white"></span>
              </div>
              <div class="flex items-center justify-between mb-4">
                <span class="text-gray-400">Año</span>
                <span id="modal-year" class="text-white"></span>
              </div>
            </div>

            <!-- Tecnologías -->
            <div class="mb-8">
              <h4 class="text-lg font-semibold text-white mb-4">Tecnologías</h4>
              <div id="modal-technologies" class="space-y-4"></div>
            </div>

            <!-- Enlaces -->
            <div class="space-y-3">
              <h4 class="text-lg font-semibold text-white mb-4">Enlaces</h4>
              <div id="modal-links" class="space-y-3"></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- CTA Section -->
  <div class="bg-black py-20 border-t border-gray-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-4xl font-bold text-white mb-6">
        ¿Tienes un proyecto en mente?
      </h2>
      <p class="text-xl text-gray-400 mb-8 max-w-2xl mx-auto">
        Transformemos tu idea en una solución digital innovadora. Contactanos para discutir cómo podemos ayudarte.
      </p>
      <div class="relative group inline-block">
        <div class="absolute -inset-1 bg-gradient-to-r from-violet-600 to-pink-600 rounded-lg blur-lg opacity-75 group-hover:opacity-100 transition duration-1000 group-hover:duration-200 animate-pulse"></div>
        <a 
          href="/contacto"
          class="relative px-8 py-4 bg-black rounded-lg text-white inline-flex items-center gap-2 font-medium hover:scale-105 transition-transform duration-300"
        >
          Iniciar Proyecto
          <ArrowDown className="w-5 h-5 rotate-[-90deg]" />
        </a>
      </div>
    </div>
  </div>
</Layout>

<script define:vars={{ allProjects }}>
  // Datos de proyectos disponibles globalmente
  window.projectsData = allProjects;

  // Función para abrir modal de proyecto
  function openProjectModal(projectId) {
    const project = window.projectsData.find(p => p.id === projectId);
    if (!project) return;

    const modal = document.getElementById('project-modal');
    
    // Rellenar contenido del modal
    document.getElementById('modal-title').textContent = project.title;
    document.getElementById('modal-client').textContent = project.client;
    document.getElementById('modal-image').src = project.image;
    document.getElementById('modal-image').alt = project.title;
    document.getElementById('modal-duration').textContent = project.duration;
    document.getElementById('modal-year').textContent = project.year;
    
    // Descripción con párrafos
    const description = document.getElementById('modal-description');
    description.innerHTML = project.fullDescription.split('\n\n').map(p => `<p class="mb-4">${p.trim()}</p>`).join('');
    
    // Características
    const features = document.getElementById('modal-features');
    features.innerHTML = project.features.map(feature => 
      `<li class="flex items-center gap-2 text-gray-300">
        <svg class="w-5 h-5 text-green-400 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        ${feature}
      </li>`
    ).join('');
    
    // Estado
    const statusElement = document.getElementById('modal-status');
    statusElement.textContent = project.status;
    statusElement.className = `px-3 py-1 rounded-full text-xs font-medium ${
      project.status === 'Completado' ? 'bg-green-500/20 text-green-400 border border-green-500/30' :
      project.status === 'En Producción' ? 'bg-blue-500/20 text-blue-400 border border-blue-500/30' :
      'bg-orange-500/20 text-orange-400 border border-orange-500/30'
    }`;
    
    // Tecnologías
    const technologies = document.getElementById('modal-technologies');
    let techHTML = '';
    Object.entries(project.technologies).forEach(([category, techs]) => {
      techHTML += `
        <div>
          <h5 class="text-violet-400 font-medium mb-2 capitalize">${category}:</h5>
          <div class="flex flex-wrap gap-2 mb-2">
            ${techs.map(tech => `<span class="px-2 py-1 bg-gray-700/50 text-gray-300 text-xs rounded">${tech}</span>`).join('')}
          </div>
        </div>
      `;
    });
    technologies.innerHTML = techHTML;
    
    // Enlaces
    const links = document.getElementById('modal-links');
    let linksHTML = '';
    if (project.github) {
      linksHTML += `<a href="${project.github}" target="_blank" rel="noopener noreferrer" class="flex items-center gap-2 p-3 bg-gray-800/50 rounded-xl text-gray-300 hover:text-white transition-colors">
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
        Ver Código
      </a>`;
    }
    if (project.demo) {
      linksHTML += `<a href="${project.demo}" target="_blank" rel="noopener noreferrer" class="flex items-center gap-2 p-3 bg-gray-800/50 rounded-xl text-gray-300 hover:text-white transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg>
        Ver Demo
      </a>`;
    }
    if (project.playStore) {
      linksHTML += `<a href="${project.playStore}" target="_blank" rel="noopener noreferrer" class="flex items-center gap-2 p-3 bg-gray-800/50 rounded-xl text-gray-300 hover:text-white transition-colors">
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M3,20.5V3.5C3,2.91 3.34,2.39 3.84,2.15L13.69,12L3.84,21.85C3.34,21.6 3,21.09 3,20.5M16.81,15.12L6.05,21.34L14.54,12.85L16.81,15.12M20.16,10.81C20.5,11.08 20.75,11.5 20.75,12C20.75,12.5 20.53,12.92 20.18,13.18L17.89,14.5L15.39,12L17.89,9.5L20.16,10.81M6.05,2.66L16.81,8.88L14.54,11.15L6.05,2.66Z"/></svg>
        Play Store
      </a>`;
    }
    links.innerHTML = linksHTML;
    
    // Testimonial
    if (project.testimonial) {
      const testimonialElement = document.getElementById('modal-testimonial');
      testimonialElement.style.display = 'block';
      testimonialElement.innerHTML = `
        <blockquote class="text-gray-300 italic mb-4">"${project.testimonial.text}"</blockquote>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 bg-gradient-to-r ${project.gradient} rounded-full"></div>
          <div>
            <p class="text-white font-medium">${project.testimonial.author}</p>
            <p class="text-gray-400 text-sm">${project.testimonial.position}</p>
          </div>
        </div>
      `;
    } else {
      document.getElementById('modal-testimonial').style.display = 'none';
    }
    
    // Mostrar modal sin bloquear scroll interno
    modal.classList.remove('hidden');
    // Solo agregamos padding al body para evitar jump del layout, pero no bloqueamos scroll
    document.body.style.paddingRight = getScrollbarWidth() + 'px';
  }

  // Función para cerrar modal
  function closeProjectModal() {
    document.getElementById('project-modal').classList.add('hidden');
    document.body.style.paddingRight = '';
  }

  // Función auxiliar para obtener el ancho del scrollbar
  function getScrollbarWidth() {
    const outer = document.createElement('div');
    outer.style.visibility = 'hidden';
    outer.style.width = '100px';
    outer.style.msOverflowStyle = 'scrollbar';
    document.body.appendChild(outer);
    
    const widthNoScroll = outer.offsetWidth;
    outer.style.overflow = 'scroll';
    
    const inner = document.createElement('div');
    inner.style.width = '100%';
    outer.appendChild(inner);
    
    const widthWithScroll = inner.offsetWidth;
    outer.parentNode.removeChild(outer);
    
    return widthNoScroll - widthWithScroll;
  }

  // Filtro de proyectos
  document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('[data-category]');
    const projectCards = document.querySelectorAll('.project-card'); // Cambiado selector específico
    
    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        const category = button.dataset.category;
        
        // Actualizar botones activos
        filterButtons.forEach(btn => {
          btn.classList.remove('bg-violet-500/20', 'text-violet-400', 'border-violet-500/50');
        });
        button.classList.add('bg-violet-500/20', 'text-violet-400', 'border-violet-500/50');
        
        // Filtrar proyectos
        projectCards.forEach(card => {
          if (category === 'Todos' || card.dataset.category === category) {
            card.style.display = 'block';
            card.style.animation = 'fadeIn 0.5s ease-out';
          } else {
            card.style.display = 'none';
          }
        });
      });
    });
    
    // Activar "Todos" por defecto
    const todosButton = Array.from(filterButtons).find(btn => btn.dataset.category === 'Todos');
    if (todosButton) {
      todosButton.click();
    }
  });

  // Hacer funciones globales
  window.openProjectModal = openProjectModal;
  window.closeProjectModal = closeProjectModal;

  // Cerrar modal con ESC
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      closeProjectModal();
    }
  });
</script>

<style>
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Scrollbar personalizado para el modal */
  .overflow-y-auto {
    scrollbar-width: thin;
    scrollbar-color: rgba(139, 92, 246, 0.3) rgba(55, 65, 81, 0.1);
  }

  .overflow-y-auto::-webkit-scrollbar {
    width: 8px;
  }

  .overflow-y-auto::-webkit-scrollbar-track {
    background: rgba(55, 65, 81, 0.1);
    border-radius: 4px;
  }

  .overflow-y-auto::-webkit-scrollbar-thumb {
    background: rgba(139, 92, 246, 0.3);
    border-radius: 4px;
  }

  .overflow-y-auto::-webkit-scrollbar-thumb:hover {
    background: rgba(139, 92, 246, 0.5);
  }

  /* Asegurar que el modal sea scrolleable */
  #project-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }

  #project-modal > div:first-child {
    height: 100%;
    overflow-y: auto;
  }

  /* Smooth scrolling */
  html {
    scroll-behavior: smooth;
  }

  /* Mobile scrolling improvements */
  @media (max-width: 768px) {
    .overflow-y-auto {
      -webkit-overflow-scrolling: touch;
    }
  }
</style>